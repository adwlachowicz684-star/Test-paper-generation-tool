# 交接说明

本项目**唯一需要读的入口文档**。换窗口、换人接手时，先看这一份。

生成方式：`python3 tools/_gen_handoff.py`（数字全部现算，不会漂移）

---

## 一、项目是什么

面向高三学生的**组卷 + 间隔复习**桌面应用（Tauri 2.0 + Python + 零构建前端）。

核心能力：从教辅 PDF 人工审核录题 → 按知识点组卷 → 导出打印 → 在线练习自动判分 →
按记忆曲线安排复习。

**根目录**：`/data/workspace/gaokao-paper`

---

## 二、当前进度（实时值）

| 项目 | 数值 |
|---|---:|
| 题库题数 | **184** 题 |
| 教辅原始素材 | 1395 题（`ref_bank.json`） |
| 已录题型 | 55 个 |
| 老高考已排除 | 40 题 |
| 重复跳过 | 15 题 |
| 人工跳过 | 17 题（其中 5 条与重复重叠，净 12 条） |
| **待录入** | **1157** 题，分 61 批 |

已录题型编号（不连续）：

```
T001~T023  T066~T068  T073~T078  T081~T103
```

### 状态等式（核对用）

```
1408 = 184(已录，含补录 13) + 40(不录) + 15(重复) + 12(跳过) + 1157(待录)
```

**1408 而不是 1395**：差的 13 是「补录题」——原 PDF 里答案写在详解末尾、
无【答案】标记，被提取器过滤，后据详解补录。它们的 key 不在 ref_bank 里。

---

## 三、继续工作的命令

```bash
cd /data/workspace/gaokao-paper

# 1. 看当前该录哪批（★ 每次开工先跑）
python3 tools/make_plan.py

# 2. 批次落盘审计（★ 必跑，见第五节）
python3 tools/audit_batches.py

# 3. 录完一批后
python3 tools/selftest.py            # 后端回归
node tools/test.mjs                  # 前端回归
python3 tools/check_skill_docs.py    # skill 文档自检
python3 tools/check_render.py        # 逐题两端核对
```

### 标准录入流程（八步）

完整版见 README「七之三、AI 录入流程」与 `skill/references/00-core.md`。

```
1. 定位原文   按题型 ID 查 tools/_toc_pages.json 找页码，读到【答案】+【详解】
2. 重建题干   矢量括号补回来，公式写成规范 LaTeX
3. 独立验算   不抄原书答案，数值扫描 / 解析降维 / 取等回代
4. 入库       hand_input.add_many（整批校验，任一题有问题整批拒绝）
5. 导出样卷   HTML + Word
6. 逐题核对   check_render.py 用题干锚点确认两端都存在
7. 跑回归     后端 / 前端 / 文档自检
8. 交付       HTML、Word 试卷、Word 答案卷、Excel
```

**第 3 步不能省**。已抓出至少 8 处原书答案错误，例如：

| 题 | 原书 | 实际 | 错因 |
|---|---|---|---|
| T019-V3 | $\frac{5\sqrt5}{2}$ | $\sqrt5+\sqrt{10\sqrt5}$ | 提取 $\sqrt5$ 时漏了分子 |
| T023-E1 | $(0,\frac{\sqrt6}{6}]$ | $(\frac25,\frac{\sqrt6}{6}]$ | 把 $t^2$ 的范围当成 $t$ 的 |
| T098-E1 | 综述"单调递增" | 单调递减 | 前文推导对，末尾写反 |

---

## 四、★ 未完成的缺口（接手先处理这个）

**第 8、9 批从未落盘。** 报告写过"已完成"，但录入脚本根本不存在，
题目一道都没进库。这是用"上一批 + 本批计划"推算、没有查证导致的。

实际缺口：

| 题型 | 题型名 | 待补 | key |
|---|---|---:|---|
| T070 | 一元二次复合型：函数性质综合型 | 4 | M-T-070-E1、M-T-070-V1、M-T-070-V2、M-T-070-V3 |
| T071 | 嵌套函数基础型 | 4 | M-T-071-E1、M-T-071-V1、M-T-071-V2、M-T-071-V3 |
| T072 | 嵌套函数常规型：无参双坐标系换元转换法 | 3 | M-T-072-E1、M-T-072-V1、M-T-072-V2 |

**T069 已归入"不录(老高考)"，不需要补。** 实际要补的是 T070~T072 共 11 题。

补完之后 `audit_batches.py` 的断号告警（第 1、8、9 批）可用
"第 1 批命名不统一、第 8/9 批已补录"为由忽略，或改名脚本消除告警。

---

## 五、三个防坑工具（别跳过）

### 1. `audit_batches.py` —— 防"报告完成实际没录"

静态检查两件事：批次编号断号、声称覆盖的题型在库中是否真有题。

第 8、9、15 批都出过这个问题。**每次汇报"完成"前必须跑**。

### 2. `check_render.py` —— 防"数量对但内容错位"

用题干去公式后的纯中文片段做锚点，两端各查一次。

两端都是 18 题、看着一样，实际可能是错位的——
答案卷题号坑就是这么来的（题号全 0，且顺序与试卷不一致）。

### 3. 源码泄漏扫描 —— 防 LaTeX 命令名显示在页面上

匹配输出里的命令名（`overrightarrow`、`subsetneq` 这类）。
**每换一个知识模块就会暴露一批新缺口**（录集合题时一次补了 5 个符号）。

当前 MACRO 已扩到 98 条。

---

## 六、待人工裁定的清单

### 跳过清单（17 条记录，其中 7 条需裁定）

其中 5 条与"重复"重叠；净 12 条计入等式。
其余为"重复"（与前面某题同题干同答案），无需处理。

| key | 原因 | 说明 |
|---|---|---|
| `M-T-068-V1` | 答案存疑 | 与 M-T-068-V2 同题；V1 标答"1 或 3"中"1"无来源（严格计数恒为 3），且 V1 无解析，已 |
| `M-T-073-E1` | 提取破碎 | 分段式 "a x - 1" 无法判定是 ax-1 / a^x-1 / x-a，三种解读都推不出原书答案 (-1, |
| `M-T-074-E1` | 提取破碎 | t = x + 1/(4x+1) 的形式与解析"t=1 无解"矛盾，无法重建 |
| `M-T-074-V1` | 印刷错误 | 选项 C 与 D 印刷完全相同（都是 (-1,0)∪(5/4,2)），解析被截断，无法判定开闭差异 |
| `M-T-075-E1` | 解析截断 | 解析截断在关键处，答案 (5/7,1)∪{0} 无法验证 |
| `M-T-084-V1` | 答案存疑 | $h(x)=\log_{2}x-x$ 在 $(0,+\infty)$ 无零点（$\log_2 x<x$ 恒成立） |
| `M-T-093-V1` | 待核查 | 需人工核对原 PDF 后决定是否补录 |

查看全部：`python3 tools/skipped.py pending`

### 不录清单（40 条，老高考内容）

江苏 2021 年起考新高考，以下内容不考，已从批次计划排除：

| 范围 | 内容 | 题数 |
|---|---|---:|
| T025~T036 | 不等式选讲（柯西、绝对值、分析法） | 37 |
| T069 | 线性规划型 | 3 |

**数据没删**，单列在计划清单 Excel 的「不录清单」表，想反悔随时捞回来。

判定规则集中在 `tools/old_gaokao.py`，改一处全处生效。
查看明细：`python3 tools/old_gaokao.py`

---

## 七、数据文件说明

| 文件 | 内容 | 备注 |
|---|---|---|
| `data/bank.json` | 题库正文 | 权威数据 |
| `data/ref_bank.json` | 教辅提取原文 | 录入素材，1395 题 |
| `data/kp_notes.json` | 知识点讲解 | 288 条【提分秘籍】 |
| `data/skipped.json` | 跳过清单 | 待人工裁定 |
| `data/progress.json` | 练习记录 | **重建不回来**，删题走 `batch-delete` |
| `data/_toc_pages.json` | 题型→页码映射 | 422 条，全覆盖 |
| `高三题库与间隔复习系统.xlsx` | 题库表 | 可在 Excel 改后 `sync_excel.py` 同步回库 |
| `教辅录入任务计划清单.xlsx` | 批次计划 | 6 个表，重跑 make_plan.py 刷新 |

---

## 八、skill 位置（注意会被重置）

```
/data/skills/gaokao-import/          ← 系统目录，环境重启会被清空（已发生 3 次）
/data/workspace/gaokao-paper/skill/  ← 持久副本（权威）
```

恢复命令：

```bash
cp -r /data/workspace/gaokao-paper/skill/* /data/skills/gaokao-import/
```

skill 含 SKILL.md + 10 个 reference，按来源和任务分流，
单次调用只加载 2~4 个文档（约 2.1k~9.6k token）。

---

## 九、回归基线

| 项目 | 基线 | 命令 |
|---|---:|---|
| 后端 | 455 / 455 | `python3 tools/selftest.py` |
| 前端 | 37 / 37 | `node tools/test.mjs` |
| skill 文档自检 | 106 / 106 | `python3 tools/check_skill_docs.py` |

**改动后三项都必须通过才算完成。**

---

## 十、已知限制

| 限制 | 说明 |
|---|---|
| 沙箱无法编译 exe | Rust 装不上（各源 403），但配置已用真 Tauri CLI 校验过 |
| 沙箱看不到浏览器渲染 | 装不了浏览器二进制，公式显示效果需人工打开 HTML 确认 |
| 单 exe 做不到 | Tauri 依赖系统 WebView2，架构决定 |
| 目标机需 Python | 仅导入新卷子时需要；孩子刷题、复习不需要 |
| 网页文本公式整块消失 | 组卷网这类站点公式用 MathJax 渲染，文本层是空的，**不能用来录题** |

---

## 十一、最容易踩的坑（按踩中次数排序）

| 坑 | 次数 | 后果 |
|---|---:|---|
| Python 里 LaTeX 反斜杠被转义（`\b`→退格、`\v`→制表符） | 3+ | 文件 SyntaxError，报错行号还指向下一行 |
| 中文里写 ASCII 双引号提前终止字符串 | 3 | 同上 |
| `replace(...,1)` 插到同名函数的前一个分支 | 3 | **测试全绿但功能不生效** |
| 用推算代替查证（报告"完成"实际没落盘） | 3 | 批次断号，题目没入库 |
| 文档数字漂移（README 写"216 题"实际已变） | 2 | 误导后续判断 |
| 测试断言写成"自己等于自己" | 1 | 检查项失去意义 |

前四条已写进 `skill/references/40-api.md` 和 `30-pitfalls.md`。

---

## 十二、一句话总结

项目能跑，题库 184 题可用，待录 1157 题。
接手后先补 T070~T072 的 11 题，然后按计划清单第 01 批往后走。
**每次汇报"完成"前跑 `audit_batches.py`。**
