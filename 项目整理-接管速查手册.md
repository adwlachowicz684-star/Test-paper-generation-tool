# 项目整理 · 接管速查手册

> 生成日期：2026-09-06　整理人：元宝　核对方式：实跑脚本 + 读库，非抄文档
> 定位：**给刚接手的人/AI 在 5 分钟内建立全局认知**。
> 干活细节看 `交接说明.md`，本册只讲"是什么、有多少、哪里缺、先干啥"。

---

## 一、一句话定位

面向**高三学生**的桌面应用，把「教辅 PDF → 人工审核录题 → 自动组卷 → 导出打印 → 在线练习自动判分 → 按记忆曲线复习」整条链路跑通。

最大的设计取舍：**不追求全自动识别，走"人工审核录入"**。
因为 PDF 里数学的函数括号、区间括号是矢量绘制而非文本，脚本再怎么调参也提不出来，硬提必然出错。所以默认模式是 AI 读原文 → 独立验算 → 入库，已靠这一步抓出原书至少 8 处答案错误。

---

## 二、技术栈与两条跑法

| 层 | 选型 | 说明 |
|---|---|---|
| 桌面壳 | **Tauri 2.0**（Rust） | 产物小（打包后 1.4 MB），但依赖系统 WebView2，**做不到单 exe** |
| 前端 | **零构建原生 ES Module** | 不用 React/Vue，免构建、改完刷新即可 |
| 后端 | **Python 3.9+** | 业务逻辑全在 Python，Rust 只做调度 |
| 公式渲染 | **MathML / OMML**（自研转换） | 不用 KaTeX——Word 端要原生可编辑公式，KaTeX 只能出图片 |
| 数据 | 本地 JSON | 无数据库、无网络依赖 |

**两条跑法，按需选：**

```bash
# A. 完整桌面应用（需 Rust，首次编译 3~10 分钟）
pip install pymupdf python-docx pillow openpyxl
npm install && npm run tauri dev

# B. 浏览器模式（免 Rust，日常够用）
python3 tools/dev_bridge.py     # 打开 http://localhost:8765/
```

> 工作目录固定 `/data/workspace/gaokao-paper`，所有相对路径都相对它。
> 代码内路径**必须**由 `__file__` 推导，禁止硬编码（曾因硬编码在 Windows 上 import 失败）。

---

## 三、目录速览

```
gaokao-paper/
├── src/            前端 3124 行：6 个视图 + render(693) / api / grade
├── src-tauri/      Rust 壳，仅做 Python 调度
├── py/             后端 8089 行：main(1434) / make_paper(1289) / kp_catalog(1053) / mathml(963)
├── data/           bank.json 题库 · ref_bank.json 素材 · kp_notes.json 讲解
├── skill/          录入 skill（SKILL.md + 10 个按需加载的 reference）
├── tools/          脚本：selftest / test.mjs / make_plan / audit_batches / check_render / input_batchN
└── 一键打包-*.bat  交付用打包脚本（Windows / macOS 各一份）
```

**`mathml.py`（963 行）是整个项目的技术心脏**——LaTeX → MathML/OMML 双向转换，Word 和 HTML 两端的公式都靠它。当前 MACRO 表已扩到 98 条。

六个前端视图：`compose` 组卷 / `practice` 练习 / `review` 复习 / `library` 题库 / `settings` 设置 / `stats` 统计。

---

## 四、实测数据（我跑出来的，不是抄文档的）

```bash
python3 tools/selftest.py          # 后端
node tools/test.mjs                # 前端
python3 tools/audit_batches.py     # 批次审计
python3 tools/make_plan.py         # 录入计划
```

### 题库现状

| 指标 | 实测值 |
|---|---:|
| 题库题数 | **184**（全部数学） |
| 构成 | 选择 88 / 填空 68 / 解答 28 |
| 知识点分布 | 函数与导数 101 · 不等式 58 · 集合与逻辑 25 |
| 已录题型 | **55 个**（T001~T023、T066~T068、T073~T078、T081~T103） |
| 教辅素材 | 1395 题（`ref_bank.json`） |
| 待录入 | **1157 题，分 61 批** |
| 知识点讲解 | 288 条【提分秘籍】（37 二级 / 11 一级） |

### 状态等式（已验证成立）

```
1408 = 184(已录，含补录13) + 40(不录) + 15(重复) + 12(跳过) + 1157(待录)
```

> 1408 比素材数 1395 多 13：那是"补录题"——原 PDF 里答案写在详解末尾、无【答案】标记，被提取器过滤，后据详解人工补录，key 不在 ref_bank 里。

### 回归基线 ⚠️ 与文档不符

| 项目 | 文档声称 | **实测** | 差异 |
|---|---:|---:|---|
| 后端 selftest | 455 / 455 | **454 / 454** | −1 |
| 前端 test.mjs | 37 / 37 | **37 / 37** | ✅ |
| skill 文档自检 | 106 / 106 | **102 / 102** | −4 |

三项**全部通过**，但数字比《交接说明》第九节写的少。属于文档漂移（该项目自己列的"高频坑"第 5 条就是它）。
**建议：以实测为基线，或跑一次后回填文档。** 另外 `check_skill_docs.py` 会把 skill 路径硬编码为 `/data/skills/gaokao-import`，该目录环境重启会清空，**每次跑前先恢复**：

```bash
mkdir -p /data/skills/gaokao-import && cp -r skill/* /data/skills/gaokao-import/
```

---

## 五、★ 接手第一件事：补 11 道题

**第 8、9 批从未落盘**——历史报告写过"已完成"，但录入脚本根本不存在，题目一道没进库。
这是典型的"用上一批 + 本批计划推算、没查证"，项目自己已把它列为一号坑。

| 题型 | 题型名 | 待补 | key |
|---|---|---:|---|
| T070 | 一元二次复合型：函数性质综合型 | 4 | M-T-070-E1 / V1 / V2 / V3 |
| T071 | 嵌套函数基础型 | 4 | M-T-071-E1 / V1 / V2 / V3 |
| T072 | 嵌套函数常规型：无参双坐标系换元转换法 | 3 | M-T-072-E1 / V1 / V2 |

已核实：**11 题素材在 `ref_bank.json` 里齐全，可直接补录**。
T069（线性规划）属老高考内容，已判定不录，不用管。

补完后 `audit_batches.py` 的断号告警（第 1、8、9 批）可用"第 1 批命名不统一、第 8/9 批已补录"为由忽略。

### 八步标准录入流程

```
1. 定位原文   按题型 ID 查 tools/_toc_pages.json 找页码，读到【答案】+【详解】
2. 重建题干   矢量括号补回来，公式写成规范 LaTeX
3. 独立验算   ★ 不抄原书答案，自己算一遍
4. 入库       hand_input.add_many（整批校验，任一题有问题整批拒绝）
5. 导出样卷   HTML + Word
6. 逐题核对   check_render.py 用题干锚点确认两端都存在
7. 跑回归     后端 / 前端 / 文档自检
8. 交付       HTML、Word 试卷、Word 答案卷、Excel
```

**第 3 步不能省。** 已抓出的原书错误举例：

| 题 | 原书答案 | 实际 | 错因 |
|---|---|---|---|
| T019-V3 | $\frac{5\sqrt5}{2}$ | $\sqrt5+\sqrt{10\sqrt5}$ | 提取 $\sqrt5$ 时漏了分子 |
| T023-E1 | $(0,\frac{\sqrt6}{6}]$ | $(\frac25,\frac{\sqrt6}{6}]$ | 把 $t^2$ 的范围当成 $t$ 的 |
| T098-E1 | 综述"单调递增" | 单调递减 | 前文推导对，末尾写反 |

---

## 六、三个防坑工具（别跳过）

| 工具 | 防的是什么 |
|---|---|
| `audit_batches.py` | 防"报告完成实际没录"（第 8、9、15 批都栽过）。**每次汇报"完成"前必跑** |
| `check_render.py` | 防"数量对但内容错位"。两端都是 18 题、看着一样，实际可能错位 |
| 源码泄漏扫描 | 防 LaTeX 命令名（`overrightarrow` 等）当正文显示在页面上。每换知识模块会暴露一批新缺口 |

---

## 七、待人工裁定（7 条）

跳过清单 17 条，其中 5 条与"重复"重叠、净 12 条计入等式。真正需要人拍板的 7 条：

| key | 类型 | 一句话 |
|---|---|---|
| M-T-068-V1 | 答案存疑 | 标答"1 或 3"中"1"无来源（严格计数恒为 3），已录 V2 |
| M-T-073-E1 | 提取破碎 | "a x - 1" 无法判定是 ax−1 / aˣ−1 / x−a |
| M-T-074-E1 | 提取破碎 | t = x + 1/(4x+1) 与解析"t=1 无解"矛盾 |
| M-T-074-V1 | 印刷错误 | 选项 C 与 D 印得完全一样，无法判定开闭 |
| M-T-075-E1 | 解析截断 | 截断在关键处，答案无法验证 |
| M-T-084-V1 | 答案存疑 | h(x)=log₂x−x 在 (0,+∞) 无零点，原书答案不成立 |
| M-T-093-V1 | 待核查 | 需人工核对原 PDF 后决定是否补录 |

查看全部：`python3 tools/skipped.py pending`

**不录清单 40 题**（江苏 2021 起考新高考）：T025~T036 不等式选讲 37 题 + T069 线性规划 3 题。
**数据没删**，单列在计划清单 Excel 的「不录清单」表，想反悔随时捞回。判定规则集中在 `tools/old_gaokao.py`。

---

## 八、仓库层面的整理建议

这次上传是 **3 个 zip 套娃**（README.zip / 交接说明.zip / 高考题库完整包.zip），
GitHub 无法在线预览、无法 diff、别人 fork 也看不到内容。建议改成扁平结构直接传文件树：

```
Test-paper-generation-tool/
├── README-高考组卷项目说明.md     ← 保留这份（最新版）
├── 交接说明.md                    ← 唯一入口文档
├── 项目整理-接管速查手册.md        ← 本册
├── 教辅录入任务计划清单.md
└── gaokao-paper/                 ← 整个项目目录原样上传
```

同时发现两个冗余，建议处理：

| 问题 | 说明 | 建议 |
|---|---|---|
| **两份 README 不一致** | 外层 `README-高考组卷项目说明.md`（3052 行）比内层 `gaokao-paper/README.md`（2880 行）多出「七之三、AI 录入流程」整节。外层是新版，内层是旧版 | 删掉内层旧版，只留外层；《交接说明》引用「七之三」即指外层 |
| **可清理文件 12.9 MB** | `data/*.bak`×15、`data/_backup_*`×6、`src/slices/`×154（0 道题引用、可从 PDF 重建）、`html/`+`out/`×45（可重新生成） | 清理后再上传；打包时本就排除这些，交付包仅 1.4 MB |

---

## 九、已知限制（别踩）

| 限制 | 说明 |
|---|---|
| 沙箱编译不了 exe | Rust 各源 403，但配置已用真 Tauri CLI 校验过 |
| 沙箱看不到浏览器渲染 | 公式显示效果需人工打开 HTML 确认 |
| 做不到单 exe | Tauri 依赖系统 WebView2，架构决定 |
| 目标机需装 Python | 仅**导入新卷子**时需要；孩子刷题、复习不需要 |
| 网页文本公式整块消失 | 组卷网这类站点公式用 MathJax 渲染、文本层是空的，**不能用来录题** |

---

## 十、下一步优先级

1. **补 T070~T072 的 11 题**（素材齐全，立即可做）
2. 按《教辅录入任务计划清单》第 01 批往后推进 1157 题
3. 回填回归基线数字（455→454、106→102）
4. 删掉内层旧 README，仓库改扁平结构
5. 裁定 7 条存疑题

> **每次汇报"完成"前跑 `audit_batches.py`。**

---

## 十一、推送到 GitHub：增量，只传变化的文件

```bash
python3 push_repo.py <本地目录> <token> -m "提交说明"
python3 push_repo.py <本地目录> <token> --dry-run   # 只看会传什么，不真传
```

### 为什么快

Git 的 blob 按内容寻址，同一份内容永远对应同一个 sha1。
脚本在本地算出每个文件的 sha1（`sha1("blob <字节数>\0" + 内容)`），
与远端比对后**只 POST 真正变化的文件**，未变的直接复用远端 blob。

实测：173 个文件的仓库，改 1 个文件就只上传 1 个。

### 注意

- **本地删了的文件会被真正删除**。用 `--dry-run` 先看一眼删除清单再推
- `LICENSE` 必须在本地目录里，否则会被判为删除（AGPL-3.0 要保留）
- 一次推送 = **一次提交**，不会把历史刷成几百条
- 令牌用完即删，下次需要时再给
