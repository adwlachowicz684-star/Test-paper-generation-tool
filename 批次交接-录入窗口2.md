# 批次交接包 · 录入窗口2

**把下面「三、可直接复制的指令」整段发给另一个窗口即可。**

---

## 一、这段批次是什么

| 题型 | 名称 | 知识点 | 题号 | 页码 |
|---|---|---|---|---|
| M-T-009 | “1”的代换型 | 不等式 / 基本不等式 | V1、V2（2 题） | p230 起 |
| M-T-046 | 数值逼近 | 函数与导数 / 幂指对比较大小 | E1、V1、V2（3 题） | p017–p019 |
| M-T-047 | 中心对称性质1：几个复杂的奇函数 | 函数与导数 / 对称性与周期性 | E1、V1、V2、V3（4 题） | p020 起 |
| M-T-048 | 中心对称性质2：与三角结合的中心对称 | 函数与导数 / 对称性与周期性 | E1、V1、V2、V3（4 题） | p020 |

共 **13 题**。已用 `claim.py` 认领，别的窗口领不到。

---

## 二、并发安全（这次专门加固过）

| 文件 | 谁写 | 会不会被覆盖 |
|---|---|---|
| `tools/input_batchXX.py` | 你独占的文件名 | 不会 |
| `tools/_claims/录入窗口2.json` | 只你写 | 不会（**每个窗口独立文件**） |
| `data/bank.json` | **你不写** | — |
| `data/skipped.json` | **你不写** | — |

**不要用 xlsx 标注进度。**《教辅录入任务计划清单.xlsx》是 `make_plan.py` 的
生成物，每次跑都整份重写，手工标注必然丢失。真正的进度在 `bank.json`，
由合并窗口统一写。

需要标记「跳过」时：

```bash
python3 tools/claim.py 录入窗口2 --skip M-T-047-V2 --reason 答案存疑
```

写进你自己的 `_claims/录入窗口2_skip.json`，合并窗口统一并入。

---

## 三、可直接复制的指令

```
你在参与一个高考数学题库的并行录入项目。项目根目录 gaokao-paper/。

【你的任务】
录入这 4 个题型共 13 题：
  M-T-009   “1”的代换型        V1、V2        PDF 第230页起
  M-T-046   数值逼近           E1、V1、V2    PDF 第17-19页
  M-T-047   中心对称性质1       E1、V1、V2、V3  PDF 第20页起
  M-T-048   中心对称性质2       E1、V1、V2、V3  PDF 第20页
PDF：gaokao-paper/原件/2024高中数学热点题型归纳完整解析版.pdf

【读原文，不要下载 PDF】
  cat gaokao-paper/原件/按页原文/p230.txt
  cat gaokao-paper/原件/按页原文/p017.txt   # 类推 p018 p019 p020
页码带 ? 的就搜特征词：
  python3 tools/dump_pdf.py <pdf> --find '题干里的特征短句' --context 1200

【符号存疑时才用 PDF】
  python3 tools/dump_pdf.py <pdf> --pages 19 --chars    # 注意 --pages 是 0-based

【产出】
只写一个文件：gaokao-paper/tools/input_batch18W2.py
格式照抄 tools/input_batch15c.py（含 QS 列表、每题 review 字段、r"" 字符串）。

【关键纪律 —— 违反会毁掉别人的成果】
1. 绝不执行入库命令，绝不写 data/bank.json。只写 input_batch18W2.py。
2. 不要跑 commit_batch*.py、run_batch.py、make_plan.py——它们会写共享文件。
3. 不要在 xlsx 里标注进度（那是生成物，会被覆盖）。
4. 需要标记跳过：python3 tools/claim.py 录入窗口2 --skip <题号> --reason <原因>

【每题必做】
- 教辅 PDF 的数学符号是私用区字符，常规提取会丢根号、上标、绝对值。
  遇到读不通的式子，用 --chars 看字符码位再确认。
- 分数和根号是矢量绘制，文本层里没有，只能靠详解反推。
- 每题的 review 字段写清：提取文本长什么样、依据什么还原、数值校验结果。
- 发现原书答案有误：答案照原书录，把结论写进 review，不要自己改答案。

【完工】
1. python3 -c "import ast;ast.parse(open('tools/input_batch18W2.py').read())"  验证语法
2. 报告：录了几题、哪些跳过、哪些原书答案存疑
```

---

## 四、另一个窗口交回后，你这边怎么收

```bash
cd gaokao-paper

# 1. 合并它产出的文件（唯一入库入口）
python3 tools/merge_batches.py input_batch18W2 --batch 18

# 2. 把它标记的跳过并入 skipped.json
python3 tools/merge_batches.py --apply-skip

# 3. 验收
python3 tools/run_batch.py 18 --no-commit --title '第18批'

# 4. 释放它的认领（或让它自己 release）
python3 tools/claim.py 录入窗口2 --release
```

`merge_batches.py` 一次 load → 全部追加 → 一次 save，
ID 由同一次分配，**不会和别的窗口撞号**。
已入库的题会自动跳过，重复跑也安全。
